# BloomTO ETL — Python dependencies. `>=` pins (not `==`) so the same
# requirements.txt installs on the workstation (Python 3.12) and prod
# (Python 3.10), where the latest available numpy version differs.
# Daily signals refresh on prod uses requests + shapely; full rebuild on
# the workstation needs the full set including pyproj + pyshp + numpy.
anthropic>=0.125.0
certifi>=2024.0.0
charset-normalizer>=3.0
et_xmlfile>=2.0
idna>=3.0
ijson>=3.0
numpy>=2.0,<3
openpyxl>=3.1
pyproj>=3.6
pyshp>=2.3
requests>=2.31
shapely>=2.0,<3
urllib3>=2.0
xlrd>=2.0
